name: Ping Deployment

on:
  schedule:
    - cron: '0 0 */4 * *' 
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping URL
        run: |
          curl -m 10 -sS https://prompt2post-aditi.streamlit.app/ > /dev/null
